# Render Blueprint for DevHub
# This file defines the complete deployment configuration

services:
  # Backend API Service
  - type: web
    name: devhub-backend
    runtime: node
    region: oregon
    plan: free
    branch: main
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: MONGO_URI
        value: mongodb+srv://oohareddy6362_db_user:sB5Y9CqbvscifAEr@link2.fxh37vr.mongodb.net/devhub?retryWrites=true&w=majority&appName=link2
      - key: JWT_SECRET
        value: your_jwt_secret_key_change_this_in_production_12345
      - key: JWT_EXPIRE
        value: 7d
      - key: FRONTEND_URL
        sync: false  # Set this manually to your frontend URL

  # Frontend Web Service
  - type: web
    name: devhub-frontend
    runtime: node
    region: oregon
    plan: free
    branch: main
    rootDir: frontend
    buildCommand: npm install && node setup-env.js && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: NEXT_PUBLIC_API_URL
        value: https://devhub-7.onrender.com/api
